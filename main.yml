# ansible-playbook -i hosts -K -v main.yml
---
  - name: Executing Roles
    hosts: all
    become: true
    roles:
      - homepage
      # - grafana-monitoring
      # - netboot.xyz
      # - docker
      # - portainer
      # - pyenv
