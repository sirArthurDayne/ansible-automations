# ansible-playbook -i hosts -K -v
---
  - name: Executing Roles
    hosts: all
    become: true
    roles:
      - grafana-monitoring
      # - netboot.xyz
      # - docker
      # - portainer
      # - pyenv
