# ansible-playbook -i hosts -K -v main.yml
---
  - name: Executing Roles
    hosts: all
    become: true
    roles:
      - traefik
      # - grafana-monitoring
      # - homepage
      # - netboot.xyz
      # - docker
      # - portainer
      # - pyenv
