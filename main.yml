# ansible-playbook -i hosts -K -v main.yml
---
  - name: Executing Roles
    hosts: all
    become: true
    roles:
      # - system/ubuntu
      # - docker
      # - portainer
      - traefik
      # - netboot.xyz
      # - pyenv
      # - homepage
      # - grafana-monitoring
